In order to run the code, run the graph.class file by typing "java graph" in the command prompt whilst being in the directory where the files are stored.
After executing the code, the occurences of every alphabet character are printed out alongside the number of times non-alphabetic characters have occured.
